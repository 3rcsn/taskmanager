= Task Manager API Documentation
:toc: left
:sectnums:

== Introduction
This is the documentation for the Task Manager API.

== Tasks

=== Create Task
A `POST` request is used to create a new task.

operation::create-task[snippets='http-request,request-fields,http-response,response-fields']

=== Get Task By ID
A `GET` request is used to retrieve a task by its ID.

operation::get-task-by-id[snippets='http-request,path-parameters,http-response,response-fields']
